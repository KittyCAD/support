name: "Support Ping : Issue Opened"

on:
  push:
  #issues:
  #types: [opened]

env:
  NUMBER: ${{ github.event.number }}

jobs:
  Notify:
    runs-on: ubuntu-latest
    steps:
    - name: Grab Author
      run: | 
        curl \
        -H "Accept: application/vnd.github+json" \
        -H "Authorization: token <TOKEN>" \
        https://api.github.com/repos/KittyCAD/suppport/issues/0 \
        | jq -r '.user' > USER_NAME
        echo ${USER_NAME}
        #https://api.github.com/repos/KittyCAD/suppport/issues/0 #${{ NUMBER }}
  
        #- name: Send Mail
        #uses: dawidd6/action-send-mail@v3
        #with:
        #server_address: smtp.gmail.com
        #server_port: 465
        #subject: "SUPPORT: New Issue Ticket $NUMBER by @"
        #to: support@kittycad.io
        #body: |
        #  "DO NOT REPLY HERE, AUTOGENERATED.\nLink: https://github.com/KittyCAD/support/$NUMBER"
